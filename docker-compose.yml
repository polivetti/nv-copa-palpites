services:
  app:
    build: .
    container_name: nv-copa
    restart: unless-stopped
    environment:
      ADDR: ":8080"
      DATABASE_PATH: "/data/copa.db"
      TZ: "America/Sao_Paulo"
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
      - ./backups:/backups
